query GetPlantingModelsQuery($planter: String!) {
    models(where: { planter: $planter }) {
        id
        planter {
            id
        }
        price
        country
        lastFund
        lastPlant
        status
        start
        lastReservePlant
        createdAt
        updatedAt
    }
}
