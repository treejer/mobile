# SwitchNetwork

> This component does return modal, user can switch between networks

- ConfirmationNetWork: detail about selected network and 'yes' or 'no' buttons for switch network.
- NetworkItem: switchable networks
- SelectNetwork: list of networks, does return NetworkItem
- SwitchNetwork: modal dose return ConfirmationNetWork and SelectNetwork
